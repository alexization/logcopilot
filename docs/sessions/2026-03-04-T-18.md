# Session Task Note - T-18

## Metadata
- Date: 2026-03-04
- Issue: #43
- Branch: `feature/43-t18-rate-limit-alert-storm`
- Task ID: T-18
- Related spec: `docs/specs/2026-03-04-mvp-v1-post-t11-delivery-plan.md`

## 1. Intent Check
- Do now:
  - ingest 경로(`/v1/ingest/events`, `/v1/ingest/otlp/logs`)에 rate-limit을 적용하고 초과 시 429를 반환한다.
  - alert 전송 경로에 cooldown/quiet-hours/rate-budget 정책을 적용하고 incident ingest 경로와 연계한다.
  - alert storm 회귀 테스트를 추가하고 운영 기본값/변경 경로를 문서화한다.
- Do not do now:
  - Admin UI 구현(T-19/T-20).
  - SQLite 영속화/secret 저장소 전환(T-21).
  - 신규 OpenAPI 엔드포인트 추가.
- Done means:
  - ingest 한도 초과 시 `ErrorResponse(code=too_many_requests)`로 429 반환.
  - alert dispatch가 cooldown/quiet-hours/rate-budget을 적용하고 suppressed/dispatched/failure를 audit로 남긴다.
  - AC6 매핑 테스트와 필수 게이트(`check/test/build`)가 모두 green.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - `product-direction -> openapi -> architecture-guardrails -> workflow -> active spec` 순으로 확인.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Loki-only ingest 안정화 + Slack/Email alert 운영 안정성 강화 범위로 MVP in-scope.
- AC-to-rule mapping to execute:
  - AC6 -> Integration/Security -> `./gradlew test --tests "*Ingest*RateLimit*Test" --tests "*Alert*Cooldown*Test"` -> PASS
  - Gate -> `./gradlew check` -> PASS
  - Gate -> `./gradlew test` -> PASS
  - Gate -> `./gradlew build` -> PASS

## 2. Plan (Short)
1. RED: ingest 429/alert cooldown 정책 테스트를 먼저 추가한다.
2. GREEN: `IngestRateLimiter`, `AlertStormPolicy+dispatch` 구현 및 incident->alert 연계를 적용한다.
3. REFACTOR: 딴지 지적(중복 idempotency quota, 서비스 간 간섭, dispatch 실패 관측성)을 반영해 보강한다.

## 3. TDD Evidence
- RED tests written first:
  - `src/test/java/com/logcopilot/ingest/IngestRateLimitIntegrationTest.java`
  - `src/test/java/com/logcopilot/alert/AlertCooldownPolicyTest.java`
- RED failure output summary:
  - `./gradlew test --tests "*IngestRateLimitIntegrationTest" --tests "*AlertCooldownPolicyTest"` 실행 시
    - `AlertStormPolicy`, `DispatchIncidentAlertCommand`, `AlertDispatchResult` 미존재로 `compileTestJava` 실패.
- GREEN pass output summary:
  - `./gradlew test --tests "*Ingest*RateLimit*Test" --tests "*Alert*Cooldown*Test"` -> PASS
  - 딴지 반영 보강 후 `./gradlew test --tests "*Ingest*RateLimit*Test" --tests "*Alert*Cooldown*Test" --tests "*IncidentAlertDispatchFailureTest"` -> PASS
- REFACTOR summary:
  - 동일 `Idempotency-Key` 재시도는 rate-limit quota를 추가 소모하지 않도록 보정.
  - cooldown/rate-budget scope를 `project+service`로 분리해 서비스 간 간섭 제거.
  - dispatch 실패를 `alert.dispatch.failed` audit로 기록하고 `resource_type=incident`로 의미 정렬.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/ingest/IngestRateLimiter.java`
  - `src/main/java/com/logcopilot/ingest/IngestController.java`
  - `src/main/java/com/logcopilot/common/error/TooManyRequestsException.java`
  - `src/main/java/com/logcopilot/common/error/GlobalExceptionHandler.java`
  - `src/main/java/com/logcopilot/alert/AlertService.java`
  - `src/main/java/com/logcopilot/incident/IncidentService.java`
  - `src/main/resources/application.properties`
  - `src/test/java/com/logcopilot/ingest/IngestRateLimitIntegrationTest.java`
  - `src/test/java/com/logcopilot/alert/AlertCooldownPolicyTest.java`
  - `src/test/java/com/logcopilot/alert/AlertCooldownIntegrationTest.java`
  - `src/test/java/com/logcopilot/incident/IncidentAlertDispatchFailureTest.java`
- Why this approach:
  - OpenAPI에 이미 정의된 ingest 429 계약을 코드로 고정하면서 기존 인증/검증 경로를 깨지 않는다.
  - alert storm 정책을 dispatch 경로 내부에서 결정해 UI/API 확장 전에도 운영 안전장치를 확보한다.
  - audit 로그에 `dispatched/suppressed/failed`를 남겨 운영 추적성을 확보한다.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*Ingest*RateLimit*Test" --tests "*Alert*Cooldown*Test"`
  - `./gradlew check`
  - `./gradlew test`
  - `./gradlew build`
- Results:
  - 모든 명령 PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Explorer sub-agent `Pauli`
- Findings:
  - High: 동일 idempotency 재시도 quota 소모 가능성.
  - High: project 단위 cooldown/rate-budget으로 서비스 간 알림 간섭.
  - Medium: incident->alert 실패 무음 처리.
  - Medium: nullable severity metadata NPE 가능성.
  - Low: dispatch audit `resource_type` 의미 불일치.
- Resolution:
  - rate-limit에 request key dedupe를 추가해 동일 idempotency 재시도는 quota 비소모 처리.
  - cooldown/rate-budget key를 `project+service` 범위로 분리.
  - `IncidentService`에서 dispatch 실패 시 `alert.dispatch.failed` audit 기록.
  - dispatch command에서 `severity_score` 필수화(0~100).
  - dispatch/suppressed/failed audit의 `resource_type`을 `incident`로 정렬.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - 현재 rate-limit은 단일 인스턴스 in-memory 윈도우라 멀티노드 환경에서 글로벌 한도 동기화는 미지원.
  - alert storm 정책 변경은 현재 `application.properties`/환경변수 경로 기반이며 UI 제어는 T-19/T-20에서 추가 필요.
- Next task ID:
  - T-19
- Suggested first check for next session:
  - UI 인증 경계/CSRF/CORS 정책을 구현할 때 alert storm 설정값 노출/변경 경로를 Admin UI에 연결할지 범위를 먼저 확정할 것.

## 9. 운영자 설정값 변경 경로 및 기본값
- Ingest rate-limit:
  - `logcopilot.ingest.rate-limit.max-requests` (기본 `120`)
  - `logcopilot.ingest.rate-limit.window` (기본 `PT1M`)
  - `logcopilot.ingest.rate-limit.max-keys` (기본 `10000`)
  - `logcopilot.ingest.rate-limit.entry-ttl` (기본 `PT1H`)
- Alert storm 제어:
  - `logcopilot.alert.storm.cooldown` (기본 `PT5M`)
  - `logcopilot.alert.storm.rate-budget` (기본 `30`)
  - `logcopilot.alert.storm.rate-window` (기본 `PT1H`)
  - `logcopilot.alert.storm.quiet-hours.enabled` (기본 `false`)
  - `logcopilot.alert.storm.quiet-hours.start` (기본 `23:00`)
  - `logcopilot.alert.storm.quiet-hours.end` (기본 `07:00`)
  - `logcopilot.alert.storm.quiet-hours.zone` (기본 `UTC`)
  - `logcopilot.alert.storm.max-scopes` (기본 `10000`)
  - `logcopilot.alert.storm.scope-retention` (기본 `PT24H`)
- 현재 MVP 단계의 변경 경로:
  - API/UI 설정 엔드포인트는 미구현(T-19/T-20 범위).
  - 서버 설정 파일(`application.properties`) 또는 동등한 환경 변수 주입으로 변경.

## 10. PR Review Follow-up (PR #44)
- Reason:
  - CodeRabbit 리뷰에서 429 재시도 힌트, 동기화 임계구역 최소화, 시계 일관성, 메모리 증가 방지, OTLP/실패 페이로드 테스트 보강 피드백이 제기됨.
- Changes:
  - `TooManyRequestsException`에 `retryAfterSeconds`를 추가하고 429 응답에 `Retry-After` 헤더를 포함.
  - `IngestRateLimiter`를 `ConcurrentHashMap + compute` 기반으로 전환해 전역 `synchronized` 병목을 제거.
  - rate-limit 상태에 TTL/최대 key 개수 제한(`max-keys`, `entry-ttl`)을 추가해 무한 증가를 방지.
  - `IncidentService.recordIngestedEvents`에서 상태 저장은 synchronized로 유지하고 alert dispatch는 락 밖에서 수행하도록 분리.
  - `AlertService`의 storm scope 상태에 retention/최대 개수 정리를 추가하고 `Instant.now()`를 `clock.instant()`로 통일.
  - 테스트 보강:
    - `IngestRateLimitIntegrationTest`에 OTLP 429 경로 및 `Retry-After` 헤더 검증 추가.
    - `IncidentAlertDispatchFailureTest`에 dispatch command(`service`, `severityScore`, `actorToken`) 캡처 검증 추가.
- Re-run verification:
  - `./gradlew test --tests "*IngestRateLimitIntegrationTest" --tests "*IncidentAlertDispatchFailureTest" --tests "*AlertCooldownPolicyTest" --tests "*AlertCooldownIntegrationTest"` => PASS
  - `./gradlew check && ./gradlew test && ./gradlew build` => PASS
