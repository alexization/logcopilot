# Session Task Note - T-00

## Metadata
- Date: 2026-03-03
- Issue: #6
- Branch: `feature/6-mvp-bootstrap-spec`
- Task ID: T-00
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Create active spec and lock upcoming task IDs.
  - Create GitHub issue and feature branch.
- Do not do now:
  - Implement API endpoints or persistence logic.
- Done means:
  - AC1~AC4 for T-00 are verifiably satisfied.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - Checked and followed.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Passed (no out-of-scope expansion).
- AC-to-rule mapping to execute:
  - AC1 -> `test -f docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`
  - AC2 -> `rg "^\\| T-0[0-9]|^\\| T-1[0-1]" docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`
  - AC3 -> `gh issue view 6 --json number,title,state,url`
  - AC4 -> `git branch --show-current`

## 2. Plan (Short)
1. Write active spec from template with AC/rules/tasks.
2. Create GitHub issue in Korean using `gh issue create`.
3. Create feature branch from `develop` using issue number.

## 3. TDD Evidence
- RED tests written first:
  - N/A (documentation/bootstrap task).
- RED failure output summary:
  - N/A.
- GREEN pass output summary:
  - `./gradlew check` => PASS
  - `./gradlew test` => PASS
- REFACTOR summary:
  - Replaced placeholder issue references with concrete `#6`.

## 4. Implementation Notes
- Key file changes:
  - Added active spec file.
  - Added this session note.
- Why this approach:
  - Enables contract-first delivery and task-by-task execution trace before feature implementation.

## 5. Verification
- Commands:
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - All PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review.
- Findings:
  - `work-unit` label does not exist in repo, so issue was created without label.
- Resolution:
  - Kept issue creation successful path first; label can be added later if repository label policy is set.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - T-01 contract tests need explicit auth strategy for `/v1/system/info`.
- Next task ID:
  - T-01.
- Suggested first check for next session:
  - Define RED tests for `/healthz`, `/readyz`, `/v1/system/info` using OpenAPI response schemas.
