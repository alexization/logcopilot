# Session Task Note - T-05

## Metadata
- Date: 2026-03-03
- Issue: #18
- Branch: `feature/18-t05-incident-apis`
- Task ID: T-05
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Incident 목록/상세/재분석 API를 OpenAPI 계약대로 구현.
  - ingest 성공 흐름에서 incident 생성 경로를 연결.
- Do not do now:
  - Analyzer 체인(rule -> llm -> fallback) 구현.
  - alerts/policy/LLM account API 구현.
- Done means:
  - `/v1/projects/{project_id}/incidents`, `/v1/incidents/{incident_id}`, `/v1/incidents/{incident_id}/reanalyze` 계약 테스트 통과.
  - 필수 게이트(`check`, `test`) 통과.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - `product-direction` -> `openapi` -> `architecture-guardrails` -> workflow 문서 순으로 확인 완료.
- MVP scope gate (`docs/domain/product-direction.md`):
  - In scope의 "Incident auto-create/list/detail/reanalyze"에 해당.
- AC-to-rule mapping to execute:
  - T-05 계약 테스트 -> `./gradlew test --tests "*IncidentEndpointsContractTest"` -> PASS
  - 회귀 테스트 -> `./gradlew test --tests "*ProjectEndpointsContractTest" --tests "*LokiConnectorEndpointsContractTest" --tests "*IngestEndpointsContractTest" --tests "*IncidentEndpointsContractTest"` -> PASS
  - 필수 게이트 -> `./gradlew check` / `./gradlew test` -> PASS

## 2. Plan (Short)
1. Incident API 계약 테스트(RED) 추가.
2. Incident 도메인/서비스/컨트롤러 구현 + ingest 연동.
3. 단위/계약 테스트와 필수 게이트 검증 후 정리.

## 3. TDD Evidence
- RED tests written first:
  - `IncidentEndpointsContractTest` 신규 추가.
- RED failure output summary:
  - `./gradlew test --tests "*IncidentEndpointsContractTest"` 실행 결과 9/9 FAIL(엔드포인트 미구현).
- GREEN pass output summary:
  - `./gradlew test --tests "*IncidentEndpointsContractTest" --tests "*IncidentServiceTest" --tests "*IngestServiceTest"` PASS.
- REFACTOR summary:
  - Incident 응답 스키마를 도메인 record로 분리.
  - ingest 성공 처리에서 incident 생성 훅을 서비스 의존성으로 분리.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/incident/IncidentController.java`
  - `src/main/java/com/logcopilot/incident/IncidentService.java`
  - `src/main/java/com/logcopilot/incident/domain/*`
  - `src/main/java/com/logcopilot/ingest/IngestService.java`
  - `src/test/java/com/logcopilot/incident/IncidentEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/incident/IncidentServiceTest.java`
  - `src/test/java/com/logcopilot/ingest/IngestServiceTest.java`
- Why this approach:
  - ingest idempotency 경로 내부에서만 incident를 생성해 중복 ingest의 중복 incident 생성을 방지.
  - Incident API는 thin controller + service orchestration 구조로 구현해 이후 T-07 analyzer 확장 지점 확보.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*IncidentEndpointsContractTest"` (RED 확인)
  - `./gradlew test --tests "*IncidentEndpointsContractTest" --tests "*IncidentServiceTest" --tests "*IngestServiceTest"`
  - `./gradlew test --tests "*ProjectEndpointsContractTest" --tests "*LokiConnectorEndpointsContractTest" --tests "*IngestEndpointsContractTest" --tests "*IncidentEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - RED 1회 실패 확인 후, 나머지 GREEN/게이트 모두 PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review (contract vs implementation diff 점검).
- Findings:
  - list cursor/limit 입력 유효성은 OpenAPI 제약 대비 보수적(normalize) 처리.
  - reanalyze는 MVP에서 동기 상태 전이까지만 구현되어 실제 비동기 job 실행은 미포함.
- Resolution:
  - T-05 범위 내에서는 계약 응답/상태코드 충족을 우선하고, analyzer/worker 연동은 T-07 이후 단계로 분리.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - incident 생성 로직은 현재 서비스 단위 단순 집계(정교한 fingerprint 병합 미적용).
- Next task ID:
  - T-06.
- Suggested first check for next session:
  - LLM account API 구현 전 Incident reanalyze 결과를 analyzer 체인(T-07)과 연결할 인터페이스 정의.

## 9. PR Review Follow-up (PR #19)
- Reason:
  - 코드리뷰에서 incident 접근 검증 흐름과 timestamp 계산 엣지 케이스가 지적됨.
- Changes:
  - `IncidentController`에서 `getIncident`/`reanalyzeIncident` 호출 시 incident의 `projectId`에 대해 `projectService.existsById` 검증을 추가.
  - `ApiMeta`를 `com.logcopilot.common.api.ApiMeta`로 추출해 공통 타입으로 정리.
  - `IncidentService`에서 `firstSeen`/`lastSeen` 계산을 단일 패스 `TimestampRange`로 변경해 `firstSeen > lastSeen` 가능성 제거.
  - `IncidentState`를 record 기반 불변 객체로 변경하고 재분석 시 새 인스턴스로 원자적 교체.
  - `Hypothesis.evidence`를 방어적 복사(`List.copyOf`)로 고정.
  - `IncidentControllerTest`를 추가해 project 존재 검증 흐름(404/202)을 검증.
- Re-run verification:
  - `./gradlew test --tests "*IncidentControllerTest" --tests "*IncidentEndpointsContractTest" --tests "*IncidentServiceTest" --tests "*IngestServiceTest"` => PASS
  - `./gradlew check` => PASS
  - `./gradlew test` => PASS
