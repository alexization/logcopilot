# Session Task Note - T-24

## Metadata
- Date: 2026-03-05
- Issue: #57
- Branch: `feature/57-admin-settings-view-ux`
- Task ID: T-24
- Related spec: `docs/specs/2026-03-04-mvp-v1-container-onboarding-completion-plan.md`

## 1. Intent Check
- Do now:
  - `/admin` 재접속 후 connector/llm/policy/alerts/token 기존값을 조회해 폼에 로드하는 API/UX를 확장한다.
  - connector/policy/alerts의 조회 GET contract를 추가하고 UI prefill 흐름을 연결한다.
  - T-24 범위에 맞는 Admin UI/contract 테스트를 RED -> GREEN으로 고정한다.
- Do not do now:
  - Loki 실연동 전환(T-25/T-26), LLM live 검증(T-28/T-29), 실발송(T-30).
  - incident lifecycle(T-31), preflight(T-32), container E2E(T-33), backup/restore(T-34).
- Done means:
  - `GET /v1/projects/{project_id}/connectors/loki`가 현재 설정(또는 미설정 상태)을 반환한다.
  - `GET /v1/projects/{project_id}/policies/export|redaction`이 현재 정책(또는 기본값)을 반환한다.
  - `GET /v1/projects/{project_id}/alerts/slack|email`이 현재 설정(또는 미설정 상태)을 반환한다.
  - `/admin`에서 connector/policies/alerts 섹션 진입/새로고침 시 기존값이 폼에 로드되고 수정 저장이 가능하다.
  - `*AdminUi*Test` + `*EndpointsContractTest` + hard gate가 통과한다.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - `product-direction -> openapi -> architecture-guardrails -> workflow -> active spec` 순으로 확인.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Loki-only, OpenAI/Gemini, Slack/Email, backend-embedded Admin UI 고정 결정을 유지하며 onboarding UX 완결도를 높이는 in-scope 작업.
- AC-to-rule mapping to execute:
  - AC2 -> UI/Contract -> `./gradlew test --tests "*AdminUi*Test" --tests "*EndpointsContractTest"` -> PASS
  - Gate -> Build/Test -> `./gradlew check && ./gradlew test && ./gradlew build` -> PASS

## 2. Plan (Short)
1. RED: `AdminUiIntegrationTest`와 endpoint contract 테스트에 설정 조회/로딩 기대값을 먼저 추가해 실패를 고정한다.
2. GREEN: connector/policy/alerts 조회 API + `/admin` prefill 로직을 최소 구현한다.
3. REFACTOR/검증: 연결 초기화/섹션 자동 refresh 흐름을 정리하고 OpenAPI 동기화 및 하드 게이트를 통과한다.

## 3. TDD Evidence
- RED tests written first:
  - `src/test/java/com/logcopilot/ui/AdminUiIntegrationTest.java`
  - `src/test/java/com/logcopilot/connector/LokiConnectorEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/policy/PolicyEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/alert/AlertEndpointsContractTest.java`
- RED failure output summary:
  - `./gradlew test --tests "*AdminUi*Test" --tests "*EndpointsContractTest"` 실행 시 11건 실패.
  - 실패 원인: connector/policy/alerts 조회 GET 엔드포인트 부재 + `app.js` 내 설정 로드 함수/호출 부재.
- GREEN pass output summary:
  - 동일 명령 재실행 결과 PASS.
  - 조회 API(contract) + UI prefill 시나리오가 모두 녹색.
- REFACTOR summary:
  - `/admin` connect 시점에 초기 인증 컨텍스트(project/token) 선로딩을 추가해 섹션별 조회 실패를 줄였다.
  - `refreshCurrentSection`에 connectors/policies/alerts 로딩 분기를 추가해 nav 진입/새로고침 동작을 일관화했다.
  - OpenAPI에 신규 GET operation 및 detail response schema를 동기화했다.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/connector/LokiConnectorService.java`
  - `src/main/java/com/logcopilot/connector/LokiConnectorController.java`
  - `src/main/java/com/logcopilot/policy/PolicyService.java`
  - `src/main/java/com/logcopilot/policy/PolicyController.java`
  - `src/main/java/com/logcopilot/alert/AlertService.java`
  - `src/main/java/com/logcopilot/alert/AlertController.java`
  - `src/main/resources/static/admin/app.js`
  - `docs/api/openapi-selfhosted-byom-v1.yaml`
  - `src/test/java/com/logcopilot/ui/AdminUiIntegrationTest.java`
  - `src/test/java/com/logcopilot/connector/LokiConnectorEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/policy/PolicyEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/alert/AlertEndpointsContractTest.java`
- Why this approach:
  - T-24 핵심은 "저장"이 아니라 "기존값 조회 후 수정" UX이므로, 저장 API 재사용만으로는 완료할 수 없다.
  - 설정 섹션별 조회 GET을 명시적으로 제공하고 UI에서 섹션 진입 시 자동 로드하도록 해 재접속 시나리오를 직접 충족했다.
  - 미설정 상태도 `configured=false`로 표준화해 UI가 오류 대신 초기 입력 상태로 동작하도록 만들었다.

## 5. Verification
- Commands:
  - `./scripts/verify-delivery-text.sh issue 57`
  - `./gradlew test --tests "*AdminUi*Test" --tests "*EndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
  - `./gradlew build`
- Results:
  - 모든 명령 PASS.

## 6. Challenge Review ("딴지")
- Run mode: parallel sub-agents on latest workspace snapshot
- Side-effect/regression reviewer: PASS (`Banach`)
- Syntax/static quality reviewer: PASS (`Ampere`)
- Task requirement/AC fit reviewer: PASS (`Nash`)
- Test correctness/completeness reviewer: PASS (`Lorentz`)
- Findings summary:
  - Blocking findings 없음.
  - 비차단: Admin UI prefill은 현재 문자열/엔드포인트 검증 중심이므로 브라우저 런타임 E2E 보강 여지 있음.
  - 비차단: 프로젝트별 설정 캐시는 수동 새로고침 전까지 이전 값을 잠시 표시할 수 있음.
- Resolution summary:
  - 민감정보 노출 방지를 위해 GET 응답에서 secret 원문을 제거하고 `*_configured` 플래그를 추가했다.
  - 저장 API에서 blank/null/omitted secret 입력 시 기존 secret 유지 로직을 반영했다.
  - `AlertController` 미사용 import(`Pattern`)를 제거했다.
- Re-run evidence after fix (gates + sub-agents):
  - `./gradlew test --tests "*AdminUi*Test" --tests "*EndpointsContractTest"` -> PASS
  - `./gradlew check && ./gradlew test && ./gradlew build` -> PASS
  - 서브에이전트 4개 도메인 최종 PASS 확인.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - Admin UI prefill 동작은 통합 브라우저 E2E로 직접 검증하지 않았으므로, 다음 작업에서 최소 1개 사용자 흐름 E2E 보강 권장.
- Next task ID:
  - T-25 (`Loki connector test 실연동화`)
- Suggested first check for next session:
  - `./gradlew test --tests "*Loki*IntegrationTest" --tests "*LokiPull*Test"`
