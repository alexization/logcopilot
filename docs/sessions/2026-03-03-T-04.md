# Session Task Note - T-04

## Metadata
- Date: 2026-03-03
- Issue: #14
- Branch: `feature/14-ingest-events-idempotency`
- Task ID: T-04
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Implement `/v1/ingest/events` contract with idempotency behavior.
  - Implement `/v1/ingest/otlp/logs` reserved behavior (`501`).
  - Add contract tests for success/failure paths.
- Do not do now:
  - Real queue/worker ingestion pipeline.
  - OTLP protobuf parsing and persistence.
  - Production-grade rate limit/payload limit middleware.
- Done means:
  - `IngestEndpointsContractTest` passes for 202/401/422/idempotency/501 paths.
- AC-to-rule mapping to execute:
  - `./gradlew test --tests "*IngestEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`

## 2. Plan (Short)
1. Write RED contract tests for ingest events and OTLP reserved endpoint.
2. Implement minimal controller/service and 501 error mapping.
3. Run verification gates and record handoff.

## 3. TDD Evidence
- RED:
  - Added `IngestEndpointsContractTest` first.
  - Initial run failed with 5 assertions (endpoints not implemented).
- GREEN:
  - Added `IngestController`, `IngestService`, `NotImplementedException`, and error handler mapping.
  - Re-ran targeted tests to PASS.
- REFACTOR:
  - Kept request validation in service layer and transport/header validation in controller.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/ingest/IngestController.java`
  - `src/main/java/com/logcopilot/ingest/IngestService.java`
  - `src/main/java/com/logcopilot/common/error/NotImplementedException.java`
  - `src/main/java/com/logcopilot/common/error/GlobalExceptionHandler.java`
  - `src/test/java/com/logcopilot/ingest/IngestEndpointsContractTest.java`
- Behavior choices:
  - Idempotency is in-memory by `Idempotency-Key` and returns previous accepted response on replay.
  - OTLP logs endpoint returns `501 not_implemented` by default as MVP reserved path.

## 5. Verification
- `./gradlew test --tests "*IngestEndpointsContractTest"` => PASS
- `./gradlew check` => PASS
- `./gradlew test` => PASS

## 6. Challenge Review ("딴지")
- Findings:
  - Idempotency storage is memory-local and reset on restart.
  - Ingest auth validation logic is still duplicated in controllers.
- Resolution:
  - Accepted for T-04 MVP scope.
  - Plan to promote auth/idempotency guards into reusable components in later task.

## 7. Next Handoff
- Remaining risk:
  - Cross-instance idempotency consistency is not guaranteed.
- Next task ID:
  - T-05.
- Suggested first check for next session:
  - Define incident list/detail/reanalyze RED tests from OpenAPI contract.

## 8. PR Review Follow-up (PR #15)
- Reason:
  - Review found potential `500` when `events` is `null` and controller maps request before validation.
  - Review also found potential `500` on `source/severity = null` and unbounded idempotency cache growth.
- Changes:
  - Made controller event mapping null-safe.
  - Added service validation for `null` items in events list.
  - Added explicit null/blank guards for `source` and `severity`.
  - Bounded idempotency cache (`max 10,000`) with eldest-entry eviction.
  - Added contract tests:
    - `ingestEventsReturns422WhenEventsIsNull`
    - `ingestEventsReturns422WhenSourceIsNull`
    - `ingestEventsReturns422WhenSeverityIsNull`
- Re-run verification:
  - `./gradlew test --tests "*IngestEndpointsContractTest"` => PASS
  - `./gradlew check` => PASS
  - `./gradlew test` => PASS
