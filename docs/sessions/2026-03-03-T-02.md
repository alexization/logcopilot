# Session Task Note - T-02

## Metadata
- Date: 2026-03-03
- Issue: #10
- Branch: `feature/10-project-apis-contract`
- Task ID: T-02
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Implement `POST /v1/projects` and `GET /v1/projects`.
  - Enforce Bearer header requirement and return standardized 401 response.
  - Add 400/409 error handling for invalid input and duplicate project names.
- Do not do now:
  - DB persistence and migrations.
  - Project update/delete APIs.
  - Connector/incident/alert API changes.
- Done means:
  - `ProjectEndpointsContractTest` passes for success and error contracts.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - Checked and followed.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Passed (project management in-scope).
- AC-to-rule mapping to execute:
  - `./gradlew test --tests "*ProjectEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`

## 2. Plan (Short)
1. Write RED tests for create/list/unauthorized/400/409 scenarios.
2. Implement minimal project controller/service and error mapping.
3. Re-run contract tests and mandatory gates.

## 3. TDD Evidence
- RED tests written first:
  - Added `ProjectEndpointsContractTest` before production implementation.
- RED failure output summary:
  - 5 tests failed due to missing project endpoints and error mappings.
- GREEN pass output summary:
  - `./gradlew test --tests "*ProjectEndpointsContractTest"` => PASS.
- REFACTOR summary:
  - Split project model (`ProjectDto`) and service (`ProjectService`) for clear responsibility.

## 4. Implementation Notes
- Key file changes:
  - `src/test/java/com/logcopilot/project/ProjectEndpointsContractTest.java`
  - `src/main/java/com/logcopilot/project/ProjectController.java`
  - `src/main/java/com/logcopilot/project/ProjectService.java`
  - `src/main/java/com/logcopilot/project/ProjectDto.java`
  - `src/main/java/com/logcopilot/common/error/BadRequestException.java`
  - `src/main/java/com/logcopilot/common/error/ConflictException.java`
  - `src/main/java/com/logcopilot/common/error/GlobalExceptionHandler.java`
- Why this approach:
  - Contract-first minimum implementation keeps API behavior explicit while deferring DB complexity.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*ProjectEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - All PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review.
- Findings:
  - Duplicate project detection currently uses case-insensitive name only.
  - In-memory storage resets on restart.
- Resolution:
  - Keep as T-02 minimal scope; move persistence/uniqueness policy hardening to downstream tasks.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - Auth validation logic is duplicated across controllers.
- Next task ID:
  - T-03.
- Suggested first check for next session:
  - Define RED tests for Loki connector upsert/test endpoints and upstream failure mapping.
