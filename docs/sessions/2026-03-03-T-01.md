# Session Task Note - T-01

## Metadata
- Date: 2026-03-03
- Issue: #8
- Branch: `feature/8-system-endpoints-contract`
- Task ID: T-01
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Implement `/healthz`, `/readyz`, `/v1/system/info` minimum contract behavior.
  - Enforce bearer header requirement for `/v1/system/info`.
  - Return standardized unauthorized error response shape.
- Do not do now:
  - JWT verification, real auth provider integration.
  - Extended system feature catalog or readiness dependency probing.
- Done means:
  - `SystemEndpointsContractTest` passes for 200/401 contract paths.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - Checked and followed.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Passed (system baseline endpoint implementation only).
- AC-to-rule mapping to execute:
  - `T-01` contract -> `./gradlew test --tests "*SystemEndpointsContractTest"`
  - hard gate -> `./gradlew check`
  - hard gate -> `./gradlew test`

## 2. Plan (Short)
1. Add RED tests for system endpoints and unauthorized path.
2. Add minimal controller and exception handler to satisfy tests.
3. Re-run contract tests then mandatory verification gates.

## 3. TDD Evidence
- RED tests written first:
  - `SystemEndpointsContractTest` added before production code.
- RED failure output summary:
  - 4 tests failed (`healthz`, `readyz`, `systemInfoRejectsMissingBearerToken`, `systemInfoReturnsRuntimeInfoWhenAuthorized`) due missing endpoint behavior.
- GREEN pass output summary:
  - `./gradlew test --tests "*SystemEndpointsContractTest"` => PASS.
- REFACTOR summary:
  - Split unauthorized response model and global exception handler into common package for reuse.

## 4. Implementation Notes
- Key file changes:
  - `src/test/java/com/logcopilot/system/SystemEndpointsContractTest.java`
  - `src/main/java/com/logcopilot/system/SystemController.java`
  - `src/main/java/com/logcopilot/common/api/ApiErrorResponse.java`
  - `src/main/java/com/logcopilot/common/error/UnauthorizedException.java`
  - `src/main/java/com/logcopilot/common/error/GlobalExceptionHandler.java`
- Why this approach:
  - Delivers OpenAPI system endpoint slice with minimum production code and clear error-shape consistency.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*SystemEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - All PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review.
- Findings:
  - Bearer token validity is format-only in this task.
  - `readyz` is static-ready and does not yet reflect external dependency health.
- Resolution:
  - Keep as MVP-minimum and track stronger auth/readiness checks in downstream tasks.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - Future tasks must keep `ErrorResponse` schema consistency across 4xx/5xx.
- Next task ID:
  - T-02.
- Suggested first check for next session:
  - Write RED tests for `POST /v1/projects` and `GET /v1/projects` response/validation contracts.
