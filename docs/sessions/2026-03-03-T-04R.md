# Session Task Note - T-04R

## Metadata
- Date: 2026-03-03
- Issue: #16
- Branch: `feature/16-pre-t05-oop-refactor`
- Task ID: T-04R
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - T-05 선행 리팩토링으로 auth 검증 중복 제거.
  - ingest 영역의 도메인 모델/정책/저장소 책임 분리.
  - 단위 테스트를 추가해 리팩토링 안전망 강화.
- Do not do now:
  - Incident API(T-05) 신규 기능 구현.
  - 영속 DB 전환, 외부 OTLP/Loki 실제 연동.
- Done means:
  - 공통 인증 컴포넌트 도입 및 모든 컨트롤러 적용 완료.
  - ingest 서비스가 도메인 정책/저장소 인터페이스를 조합하는 형태로 분리 완료.
  - 기존 계약 테스트 + 신규 단위 테스트 모두 PASS.

## 2. Plan (Short)
1. Active spec에 T-04R 작업 항목 추가.
2. auth/idempotency 공통 validator + ingest domain 분리 구현.
3. 단위 테스트 추가 후 회귀 게이트 실행.

## 3. TDD Evidence
- RED:
  - 신규 단위 테스트 추가 후 리팩토링 코드 작성.
- GREEN:
  - `./gradlew test --tests "*Ingest*Test" --tests "*BearerTokenValidatorTest"` PASS.
- REFACTOR:
  - 컨트롤러 중복 인증 검증 제거.
  - ingest의 validation/dedup/cache 책임을 domain policy + store로 분리.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/common/auth/BearerTokenValidator.java`
  - `src/main/java/com/logcopilot/common/http/IdempotencyKeyValidator.java`
  - `src/main/java/com/logcopilot/project/ProjectController.java`
  - `src/main/java/com/logcopilot/system/SystemController.java`
  - `src/main/java/com/logcopilot/connector/LokiConnectorController.java`
  - `src/main/java/com/logcopilot/ingest/IngestController.java`
  - `src/main/java/com/logcopilot/ingest/IngestService.java`
  - `src/main/java/com/logcopilot/ingest/IngestIdempotencyStore.java`
  - `src/main/java/com/logcopilot/ingest/InMemoryIngestIdempotencyStore.java`
  - `src/main/java/com/logcopilot/ingest/domain/*`
- Why this approach:
  - `springboot-patterns`: controller/service/domain 책임 분리.
  - `java-coding-standards`: 불변 도메인(record) + 명시적 정책 객체.
  - `springboot-tdd`: 리팩토링 구간을 단위 테스트로 고정.

## 5. Verification
- `./gradlew test --tests "*Ingest*Test" --tests "*BearerTokenValidatorTest"` => PASS
- `./gradlew check` => PASS
- `./gradlew test` => PASS

## 6. Challenge Review ("딴지")
- Findings:
  - ingest idempotency는 여전히 in-memory이므로 재시작 시 상태 유실.
  - project/connector 영역의 도메인 객체 분리는 후속으로 더 진행 가능.
- Resolution:
  - 현재 목표(T-05 선행 구조 정리)는 달성.
  - T-05 이후 도메인 확장 시 동일 패턴을 적용.

## 7. Next Handoff
- Remaining risk:
  - idempotency의 멀티 인스턴스 일관성은 보장되지 않음.
- Next task ID:
  - T-05.
- Suggested first check for next session:
  - incidents API도 controller thin/service orchestration/domain policy 패턴으로 시작.
