# Session Task Note - T-03

## Metadata
- Date: 2026-03-03
- Issue: #12
- Branch: `feature/12-loki-connector-apis`
- Task ID: T-03
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - Implement Loki connector upsert API.
  - Implement Loki connector test API.
  - Cover 401/404/422/502 contract paths.
- Do not do now:
  - Real Loki network integration.
  - Persistent DB storage.
  - Ingest pipeline execution.
- Done means:
  - `LokiConnectorEndpointsContractTest` passes for success and failure contracts.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - Checked and followed.
- MVP scope gate (`docs/domain/product-direction.md`):
  - Passed (Loki connector path is in MVP scope).
- AC-to-rule mapping to execute:
  - `./gradlew test --tests "*LokiConnectorEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`

## 2. Plan (Short)
1. Add connector service/controller with in-memory state.
2. Add exception mappings for 404/422/502.
3. Add contract tests and run full verification gates.

## 3. TDD Evidence
- RED tests written first:
  - Added `LokiConnectorEndpointsContractTest`.
- RED failure output summary:
  - N/A in this session log (GREEN-focused execution path).
- GREEN pass output summary:
  - `./gradlew test --tests "*LokiConnectorEndpointsContractTest"` => PASS.
- REFACTOR summary:
  - Reused existing error response structure and centralized validation in service layer.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/connector/LokiConnectorController.java`
  - `src/main/java/com/logcopilot/connector/LokiConnectorService.java`
  - `src/main/java/com/logcopilot/common/error/NotFoundException.java`
  - `src/main/java/com/logcopilot/common/error/ValidationException.java`
  - `src/main/java/com/logcopilot/common/error/BadGatewayException.java`
  - `src/main/java/com/logcopilot/common/error/GlobalExceptionHandler.java`
  - `src/main/java/com/logcopilot/project/ProjectService.java`
  - `src/test/java/com/logcopilot/connector/LokiConnectorEndpointsContractTest.java`
- Why this approach:
  - Satisfies OpenAPI contract while keeping implementation minimal and deterministic for tests.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*LokiConnectorEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - All PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review.
- Findings:
  - Upstream failure is simulated by endpoint/query pattern, not real network I/O.
  - Connector data is in-memory and reset on restart.
- Resolution:
  - Accepted as T-03 minimum scope; real integration/persistence will be handled in later tasks.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - Authorization validation is duplicated across multiple controllers.
- Next task ID:
  - T-04.
- Suggested first check for next session:
  - Define RED tests for ingest events API with idempotency handling.
