# Session Task Note - T-06

## Metadata
- Date: 2026-03-03
- Issue: #20
- Branch: `feature/20-t06-llm-account-apis`
- Task ID: T-06
- Related spec: `docs/specs/2026-03-03-mvp-v1-work-unit-bootstrap.md`

## 1. Intent Check
- Do now:
  - OpenAPI 계약 기준으로 LLM 계정 API 5개 엔드포인트를 구현한다.
  - API key/OAuth/list/delete 수명주기와 에러 코드를 테스트 우선으로 고정한다.
- Do not do now:
  - 외부 OAuth provider 실연동(토큰 교환/갱신) 구현.
  - analyzer 체인(T-07), 정책/알림(T-08/T-09) 구현.
- Done means:
  - `/v1/projects/{project_id}/llm-accounts/api-key`
  - `/v1/projects/{project_id}/llm-oauth/{provider}/start`
  - `/v1/projects/{project_id}/llm-oauth/{provider}/callback`
  - `/v1/projects/{project_id}/llm-accounts`
  - `/v1/projects/{project_id}/llm-accounts/{account_id}`
  - 위 엔드포인트 계약 테스트 및 필수 게이트(`check`, `test`) 통과.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - `product-direction` -> `openapi` -> `architecture-guardrails` -> workflow 문서 순으로 확인.
- MVP scope gate (`docs/domain/product-direction.md`):
  - In scope의 "OpenAI/Gemini account management (OAuth/API key)" 항목에 해당.
- AC-to-rule mapping to execute:
  - AC1(API key upsert) -> Integration -> `./gradlew test --tests "*LlmAccountEndpointsContractTest"` -> PASS
  - AC2(OAuth start/callback) -> Integration -> `./gradlew test --tests "*LlmAccountEndpointsContractTest"` -> PASS
  - AC3(list/delete) -> Integration -> `./gradlew test --tests "*LlmAccountEndpointsContractTest"` -> PASS
  - AC4(필수 게이트) -> Build/Test -> `./gradlew check && ./gradlew test` -> PASS

## 2. Plan (Short)
1. LLM 계정 계약/단위 테스트를 먼저 작성해 RED 실패를 확인한다.
2. 컨트롤러/서비스 최소 구현으로 계약 상태코드/응답 스키마를 맞춘다.
3. 회귀 + 필수 게이트를 실행하고 세션 노트를 정리한다.

## 3. TDD Evidence
- RED tests written first:
  - `src/test/java/com/logcopilot/llm/LlmAccountEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/llm/LlmAccountServiceTest.java`
- RED failure output summary:
  - `./gradlew test --tests "*LlmAccount*Test"` 실행 시 `LlmAccountService` 미구현으로 `compileTestJava` 실패(13 errors).
- GREEN pass output summary:
  - `./gradlew test --tests "*LlmAccount*Test"` PASS
  - `./gradlew test --tests "*LlmAccountEndpointsContractTest"` PASS
- REFACTOR summary:
  - provider 검증을 흐름별(422용/400용)로 분리.
  - OAuth state 저장/소모 로직을 서비스 내부로 캡슐화.
  - 응답 DTO를 컨트롤러 record로 분리해 snake_case 계약을 명시.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/llm/LlmAccountController.java`
  - `src/main/java/com/logcopilot/llm/LlmAccountService.java`
  - `src/test/java/com/logcopilot/llm/LlmAccountEndpointsContractTest.java`
  - `src/test/java/com/logcopilot/llm/LlmAccountServiceTest.java`
- Why this approach:
  - 기존 프로젝트 구조(thin controller + service 오케스트레이션)를 유지해 T-07 analyzer 연동 시 결합도를 낮춘다.
  - API key/OAuth 계정 키를 provider+auth_type 축으로 분리해 upsert와 중복 방지 규칙을 단순화한다.
  - 생성/연동 경로에서도 project 존재 검증(400)을 적용해 유령 project 데이터 누적을 방지한다.
  - 시크릿(api_key)은 응답 모델에서 제외해 노출 경로를 차단한다.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*LlmAccount*Test"`
  - `./gradlew test --tests "*LlmAccountEndpointsContractTest"`
  - `./gradlew test --tests "*ProjectEndpointsContractTest" --tests "*LokiConnectorEndpointsContractTest" --tests "*IngestEndpointsContractTest" --tests "*IncidentEndpointsContractTest" --tests "*LlmAccountEndpointsContractTest"`
  - `./gradlew check`
  - `./gradlew test`
- Results:
  - RED 1회 실패 확인 후 GREEN/회귀/필수 게이트 모두 PASS.
  - `./gradlew build`는 의존성/설정/멀티모듈 영향이 없어 이번 태스크에서 생략.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Self-review (OpenAPI 계약/에러 응답/프로젝트 경계 검증).
- Findings:
  - OAuth callback의 외부 provider 연동은 아직 stub URL/state 기반으로 단순화됨.
  - API key/OAuth create 경로의 project 검증 오류는 OpenAPI 응답 집합을 따라 400으로 처리된다.
- Resolution:
  - T-06 범위에서는 계약 응답과 내부 수명주기 일관성을 우선한다.
  - 실제 provider 교환/만료 갱신은 T-07 이후 analyzer/auth 확장 시점에 분리한다.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- Remaining risk:
  - OAuth state 만료 정책/TTL 및 재시도 제어가 아직 없다.
  - API key 저장은 in-memory 임시 구현이며 암호화 영속 저장이 필요하다.
- Next task ID:
  - T-07.
- Suggested first check for next session:
  - analyzer 체인(rule -> llm -> fallback) 시작 전에 `LlmAccountService`에서 provider/model 선택 인터페이스를 분리할지 검토.
