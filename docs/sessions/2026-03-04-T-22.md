# Session Task Note - T-22

## Metadata
- Date: 2026-03-04
- Issue: #51
- Branch: `feature/51-t22-operability-slo-gates`
- Task ID: T-22
- Related spec: `docs/specs/2026-03-04-mvp-v1-post-t11-delivery-plan.md`

## 1. Intent Check
- Do now:
  - `check/test/build` 하드 게이트를 최종 실행해 운영 종료 조건을 확인한다.
  - SQLite on-path 기준으로 `incident p95`, `analysis p95`, `ingest success rate`를 실측한다.
  - T-22 결과(수치/한계/리스크)를 세션 문서로 고정한다.
- Do not do now:
  - 신규 OpenAPI 엔드포인트/도메인 기능 추가.
  - MVP 고정 범위 밖 확장(신규 커넥터, SSO/SAML, 멀티노드).
  - `main`/`develop` 직접 수정.
- Done means:
  - `./gradlew check`, `./gradlew test`, `./gradlew build` 모두 PASS.
  - `incident p95 < 60s`, `analysis p95 < 120s`, `ingest success rate >= 99.5%` 실측 증거가 문서에 남는다.
  - 잔여 한계/리스크와 완화 계획이 기록된다.
- Source-of-truth order checked (`docs/domain/source-of-truth.md`):
  - `product-direction -> openapi -> architecture-guardrails -> workflow -> active spec` 순으로 확인.
- MVP scope gate (`docs/domain/product-direction.md`):
  - T-22는 기능 확장이 아닌 운영성 종료 증거 수집/검증 단계로 MVP in-scope.
- AC-to-rule mapping to execute:
  - AC9 -> Integration/Perf -> `./gradlew test --tests "*OperationalSloVerificationTest"` -> PASS
  - AC9 -> Build/Test -> `./gradlew check && ./gradlew test && ./gradlew build` -> PASS
  - AC9 -> Static/Doc -> `rg -n "incident p95|analysis p95|ingest success rate|한계|리스크" docs/sessions/*.md docs/specs/*.md` -> PASS

## 2. Plan (Short)
1. RED: SLO 계산기 미구현 상태에서 테스트를 먼저 추가하고 실패를 확인한다.
2. GREEN: SLO 계산 유틸 최소 구현 + SQLite on-path SLO 통합 테스트를 통과시킨다.
3. REFACTOR/검증: 하드 게이트 실행 후 결과를 세션 문서/스펙에 반영한다.

## 3. TDD Evidence
- RED tests written first:
  - `src/test/java/com/logcopilot/common/metrics/SloMetricsCalculatorTest.java`
  - `src/test/java/com/logcopilot/operations/OperationalSloVerificationTest.java`
- RED failure output summary:
  - `./gradlew test --tests "*SloMetricsCalculatorTest" --tests "*OperationalSloVerificationTest"` 실행 시 `SloMetricsCalculator` 미구현으로 `compileTestJava` 실패(`cannot find symbol` 7건).
- GREEN pass output summary:
  - `./gradlew test --tests "*SloMetricsCalculatorTest" --tests "*OperationalSloVerificationTest"` -> PASS
  - 실측 출력: `T22_SLO incident_p95_ms=34 analysis_p95_ms=19 ingest_success_rate=100.00%`
- REFACTOR summary:
  - 계산 로직을 `SloMetricsCalculator`로 분리해 테스트 재사용성과 가독성을 높였다.
  - percentile `NaN` 입력 방어를 추가하고, ingest 성공률 측정을 HTTP 엔드포인트 경유로 보강했다.

## 4. Implementation Notes
- Key file changes:
  - `src/main/java/com/logcopilot/common/metrics/SloMetricsCalculator.java`
  - `src/test/java/com/logcopilot/common/metrics/SloMetricsCalculatorTest.java`
  - `src/test/java/com/logcopilot/operations/OperationalSloVerificationTest.java`
- Why this approach:
  - T-22 요구는 기능 확장보다 “반복 가능한 운영 증거”가 핵심이므로, 수동 측정보다 테스트 기반 측정이 회귀 방지에 유리하다.
  - SQLite on-path를 테스트에 포함해 T-21 이후 영속화 경로를 실제 종료 게이트에서 검증한다.

## 5. Verification
- Commands:
  - `./gradlew test --tests "*SloMetricsCalculatorTest" --tests "*OperationalSloVerificationTest"`
  - `./gradlew check`
  - `./gradlew test`
  - `./gradlew build`
- Results:
  - 모든 명령 PASS.

## 6. Challenge Review ("딴지")
- Reviewer model/agent:
  - Explorer sub-agent `Kuhn` + self-review
- Findings:
  - 중: `SloMetricsCalculator.percentileMillis`가 `NaN` percentile 입력을 거부하지 못함.
  - 중: ingest 성공률 측정이 서비스 직접 호출 기반이라 HTTP 계층 회귀 탐지가 약함.
  - 하: 계산기 테스트의 경계/예외 케이스 커버리지가 부족함.
- Resolution:
  - `SloMetricsCalculator`에 `Double.isNaN(percentile)` 검증을 추가했다.
  - `OperationalSloVerificationTest` ingest 성공률을 `/v1/ingest/events` HTTP 호출 기반으로 전환했다.
  - `SloMetricsCalculatorTest`에 `NaN/null/입력 범위` 예외 케이스를 추가했다.

## 7. Commit Draft
Use `docs/templates/commit-message.template.md`.

## 8. Next Handoff
- 한계:
  - 현재 SLO 측정은 통합 테스트 단일 실행 기준이며 장시간 soak test를 대체하지 않는다.
  - 외부 LLM provider 네트워크 지연/장애를 포함한 `analysis p95` 실측은 별도 운영 환경 검증이 필요하다.
- 리스크:
  - 운영 환경의 디스크 성능/동시성 조건이 로컬 SQLite 성능보다 불리할 경우 p95가 악화될 수 있다.
  - 대량 프로젝트 동시 수집 시 ingest success rate는 rate-limit 정책값 튜닝 영향이 크다.
- 완화 계획:
  - 배포 전 환경별 부하 리허설(지속 ingest + 재분석 혼합)을 추가한다.
  - 운영 대시보드에서 p95/성공률 추세를 주기적으로 점검하고 임계치 초과 시 정책값을 조정한다.
- Next task ID:
  - PR 생성 및 리뷰 루프(codex/coderabbitai) 후 머지
- Suggested first check for next session:
  - `feature/51-t22-operability-slo-gates` 기준 PR 생성 후 자동 리뷰 코멘트 분류(필수/선택/반려)부터 시작.

## 9. PR Review Follow-up
- 현재 상태:
  - 로컬 구현/검증 완료.
  - PR 생성 완료: https://github.com/alexization/logcopilot/pull/52
  - 기준 브랜치: `develop`, 헤드 브랜치: `feature/51-t22-operability-slo-gates`
  - 현재 CI 체크: 아직 보고 없음(`gh pr checks 52`)
  - coderabbitai/codex 피드백 루프: 대기 중
